// x, y is the top left corner of the bounding box, and width and height expand to the right and down
export type HighlightBoundingBox = {
    x: number;
    y: number;
    width: number;
    height: number;
};
